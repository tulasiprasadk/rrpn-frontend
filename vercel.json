{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://rrpn-backend.vercel.app/api/$1" },
    { "source": "/api", "destination": "https://rrpn-backend.vercel.app/api" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
